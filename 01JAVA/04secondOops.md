# CONTENT => INHERITANCE, 

## INHERITANCE

INHERITANCE allow one class to inherit the features(fields & methods) of another class. `class Subclass extends Superclass {methods & fields}`. Usually we use inheritance, polymorphism and other concept of OOPs together.

**USE =>** Reusability || **Terms =>** Superclass(parent), Subclass(child)

